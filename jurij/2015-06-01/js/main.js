'use strict';

$(document).ready(function() {

  var config = {
    upID: '#top-up',
    downID: '#top-down',
    scrollSpeed: 100,
    scrollIntervalMs: 50,
    multiplier: 1
  };

  scroll.init(config);
  
});